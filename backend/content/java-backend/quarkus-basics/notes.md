# Quarkus Basics

Quarkus keeps backend boot time low, which is useful when iterating on read models and content APIs.

## Focus Areas

- Keep configuration small and obvious.
- Organize packages by domain, not by framework type alone.
- Treat Docker as part of the developer runtime, not an afterthought.
